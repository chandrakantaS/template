module.exports = function (grunt) {
	// grunt.registerTask('scripts', ['build:dev', 'build:scripts:dev', 'express', 'watch:scripts']);
	// grunt.registerTask('server', ['build:dev', 'express', 'concurrent:watchAll']);
	// grunt.registerTask('dev', ['build:dev', 'express', 'watch:app']);
	// grunt.registerTask('dev:build', ['build:dev']);
	// grunt.registerTask('prod', ['build:prod', 'express']);
	// grunt.registerTask('prod:build', ['build:prod']);
	//grunt.registerTask('default', ['scripts']);


	grunt.registerTask('default', ['build:dev']);
};
