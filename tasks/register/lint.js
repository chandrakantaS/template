module.exports = function(grunt) {
    // grunt.loadNpmTasks('grunt-newer');

    // grunt.registerTask('lint:app', ['newer:jshint:app']);
    // grunt.registerTask('lint:scripts', ['newer:jshint:scripts']);

    
};